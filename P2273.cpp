#include <stdio.h>

int main()
{
    int n, m, a, b;
    char t;
    scanf("%d%d", &n, &m);
    while (m > 0)
    {
        scanf("%d%c%d", &a, &t, &b);

        

        scanf("%c", &t);
        m--;
    }

    return 0;
}